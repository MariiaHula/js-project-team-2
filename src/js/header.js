const myButton = document.querySelector('.header-darck-night');

myButton.addEventListener('click', function () {
    // Додаємо клас "clicked" при кліку
myButton.disabled = true;
  myButton.classList.add('clicked');

  // Тут ви можете додати логіку для обробки кліку
  // ...

  // Через певний час (наприклад, 500 мілісекунд), видаляємо клас "clicked"
});

const body = document.querySelector('body')

body.innerHTML = '<p>sofkpsodffk</p>'

console.log(12)